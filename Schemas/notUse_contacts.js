// Цей файл (Schemas/contacts.js) використовується на першому етапі Потім тека Schemas і цей файл видаляється, а замість них використовується тека models і файли в ній.

// const Joi = require('joi');
// const addSchema = Joi.object({
//   name: Joi.string().required(),
//   email: Joi.string().required(),
//   phone: Joi.string().required(),
// });

// module.exports = {
//   addSchema,
// };

// Переносимо цю схему до схеми валідації, щоби вони були в одному файлі для зручності редагування
